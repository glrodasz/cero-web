// Seed for the `fixtures` data source. Empty, so an integration run starts on
// the planning page with no active session to redirect away from.
const focusSessions = []

export default focusSessions
